/**
 * Browser stub for @vyaz/core's canvas-polyfill.
 *
 * In the browser, the native Canvas API is already available,
 * so no polyfill is needed.
 */
export function registerCanvasFont(): void {}
export function enableOfficeTextMeasure(): void {}
export function disableOfficeTextMeasure(): void {}