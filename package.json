{
  "name": "vyaz-monorepo",
  "version": "0.0.1",
  "private": true,
  "workspaces": [
    "packages/core",
    "packages/renderer",
    "packages/html"
  ],
  "scripts": {
    "build": "(cd packages/core && bun run build) && (cd packages/renderer && bun run build) && (cd packages/html && bun run build)",
    "bench": "bun run bench/throughput.ts",
    "bench:tables": "bun run bench/table-throughput.ts",
    "bench:resize": "bun run bench/resize.ts",
    "docs:dev": "cd docs && bunx vitepress dev",
    "docs:build": "npm run docs:gen && cd docs && bunx vitepress build",
    "docs:gen": "npx typedoc --options typedoc.json --tsconfig tsconfig.typedoc.json --skipErrorChecking packages/core/src/index.ts packages/core/src/debug.ts packages/renderer/src/index.ts --out docs/api/references"
  },
  "devDependencies": {
    "typedoc": "^0.28.20",
    "typedoc-plugin-markdown": "^4.12.0",
    "typedoc-vitepress-theme": "^1.1.3",
    "typescript": "^5.4.0"
  }
}
