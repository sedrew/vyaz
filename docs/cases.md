---
title: Cases
aside: false
outline: false
---

# Cases

Every scenario in the renderer's golden corpus
(`packages/renderer/tests/cases/`), rendered live. Pick a case on the left, tweak
its `TextFrame` JSON, and see every render variant it declares on the right —
same call path as `cases.test.ts`.

<ClientOnly>
  <CaseExplorer />
</ClientOnly>
