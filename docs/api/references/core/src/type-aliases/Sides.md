[vyaz-monorepo](../../../index.md) / [core/src](../index.md) / Sides

# Type Alias: Sides\<T\>

> **Sides**\<`T`\> = `T` \| \[`T`, `T`\] \| \[`T`, `T`, `T`, `T`\]

Defined in: [core/src/types/TableTypes.ts:28](https://github.com/sedrew/vyaz/blob/main/packages/core/src/types/TableTypes.ts#L28)

CSS-style shorthand for a per-side value of type `T`.
`v` = all four sides; `[tb, lr]` = top/bottom, left/right; `[t, r, b, l]` =
one per side (CSS clockwise order).

## Type Parameters

### T

`T`
